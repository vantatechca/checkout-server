# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Backups / stray artifacts accumulated during manual SSH work
*.bak
*.bak.*

# Build artifacts
*.tar.gz
wpay_deploy.tar.gz

# Editor / OS
.DS_Store
*.log

# Celery Beat runtime state
celerybeat-schedule

# Node (scratch scripts, playwright, etc. — not part of the app)
node_modules/

# MaxMind GeoLite2 city database — ~70MB binary, downloaded per-environment
# via scripts/download_geolite2.py, never committed. See services/geoip.py.
data/GeoLite2-City.mmdb
data/GeoLite2-City.tar.gz
