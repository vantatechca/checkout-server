# Environment files (contain secrets)
.env
.env.*

# Keep the safe template
!.env.example

# Python virtualenv + caches
venv/
__pycache__/
*.py[cod]

# Editor/OS
.DS_Store

# Backups
*.bak
*.bak.*

# Data dumps — contain real customer PII, never commit
render_data_only*.sql
*.pg.sql
