# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Backups / stray artifacts accumulated during manual SSH work
*.bak
*.bak.*

# Build artifacts
*.tar.gz
wpay_deploy.tar.gz

# Editor / OS
.DS_Store
*.log

# Celery Beat runtime state
celerybeat-schedule

# Node (scratch scripts, playwright, etc. — not part of the app)
node_modules/

# Pre-edit template snapshots kept locally for manual rollback
templates/*-backup.html
